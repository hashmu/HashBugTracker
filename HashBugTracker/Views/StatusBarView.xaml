<UserControl x:Class="HashBugTracker.Views.StatusBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HashBugTracker.Views"
             xmlns:views="clr-namespace:HashBugTracker.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="35" d:DesignWidth="800">
    <UserControl.DataContext>
        <views:StatusBarViewModel />
    </UserControl.DataContext>
    <Grid>
        <Label Content="{Binding StatusText}" />
    </Grid>
</UserControl>
